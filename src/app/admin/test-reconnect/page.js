import TestReconnectClient from './TestReconnectClient';

export const metadata = {
  title: 'Test RECONNECT Writes | Altitud Hub',
  robots: 'noindex, nofollow',
};

export default function TestReconnectPage() {
  return <TestReconnectClient />;
}
