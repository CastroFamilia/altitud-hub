import LoginClient from './LoginClient';

export const metadata = {
  title: 'Login | Altitud Hub',
  description: 'Iniciar sesión en Altitud Hub',
};

export default function LoginPage() {
  return <LoginClient />;
}
